import React from 'react';
import { ViewStyle } from 'react-native';

interface FloatingParticlesProps {
  count?: number;
  style?: ViewStyle;
  color?: string;
}

/** Floating background particles are removed for the clean, minimal design. */
export function FloatingParticles(_props: FloatingParticlesProps) {
  return null;
}
